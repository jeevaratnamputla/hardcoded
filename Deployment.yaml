apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-server
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-server
  template:
    metadata:
      labels:
        app: api-server
    spec:
      containers:
        - name: api-server
          image: example/api-server:1.0.0
          env:
            - name: DATABASE_URL
              value: "postgres://dbadmin:SuperSecretP@ssw0rd!@db.prod.internal:5432/appdb"
            - name: STRIPE_SECRET_KEY
              value: "sk_live_51FakeStripeSecretKeyOnlyForTesting000000"
            - name: AWS_ACCESS_KEY_ID
              value: "AKIAIOSFODNN7EXAMPLE"
            - name: AWS_SECRET_ACCESS_KEY
              value: "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"
